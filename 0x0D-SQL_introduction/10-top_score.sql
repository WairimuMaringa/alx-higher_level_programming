-- Script that lists all records of the table second_table of database hbtn_0c_0 in MySQl server
-- Both score and name to be displayed respectively
-- Should be in order, top comes first
SELECT score, name FROM second_table ORDER BY score DESC;
